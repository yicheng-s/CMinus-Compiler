# 24号小组 答辩验收操作指南

---

## 答辩前 5 分钟 — 编译

打开 PowerShell，进入仓库目录：

```bash
cd D:\code\CMinus-Compiler

g++ src/Lexer.cpp src/SLRTableGenerator.cpp src/Parser.cpp src/IRGenerator.cpp src/main.cpp -o cminus.exe -std=c++17
```

编译无报错即就绪。

---

## 助教发题后 — 生成三个 .tsv

助教会通过腾讯会议发一个文件，名为 `test_24号.sy`。下载后放到仓库根目录 `D:\code\CMinus-Compiler\` 下。

在终端执行：

```bash
./cminus.exe test_24号.sy "24号"
```

运行结束后，当前目录会多出三个文件：

```
24号 lex.tsv      ← 词法分析输出（Token序列）
24号 gra.tsv      ← 语法分析输出（规约序列）
24号 ir.tsv       ← 中间代码输出（LLVM IR）
```

**把这三个文件通过腾讯会议发给助教。**

---

## 应急

| 问题 | 处理 |
|------|------|
| `Permission denied` | 关掉其他终端窗口，把 `cminus.exe` 换成 `cm.exe` 重编译 |
| 编译器报错 `[FATAL ERROR]` | 终端会打印行号和错误 Token，把信息报给助教 |
