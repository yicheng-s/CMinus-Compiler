# 🚀 编译原理大作业接力任务书

- **目标语言**：C-- (C 语言子集)
- **目标代码**：LLVM IR (.ll)
- **代码仓库**：Gitee (必须建立 main 或 master 分支)
- **开发语言**：C++（强烈推荐，因为官方提供的中端 API 是 C++ 的）
- **团队成员**：A、B、C、D

------



## 🌟 整体架构与数据流向

为了让接力顺利进行，所有人必须明确数据流向：
源代码(.sy) ➡️ **(A负责: 词法分析)** ➡️ Token流 ➡️ **(B/C负责: SLR语法分析)** ➡️ 规约序列 & 抽象语法树(AST) ➡️ **(D负责: 语义分析与Visitor)** ➡️ 中端API ➡️ LLVM IR代码(.ll)

------



## 📅 每日详细任务拆解与 DDL 安排

### 🕒 Day 1 (5月14日) - 基建与词法引擎

👨‍💻 **负责人：同学 A（组长）**
⏰ **今日 DDL：** 5月14日 23:59 前 push 到 Gitee。

**【核心任务】**

1. **Gitee 仓库初始化**：创建仓库，配置 .gitignore（忽略编译产生的 .exe, .o 等文件），邀请 B、C、D 进组。
2. **词法分析器复用与改造**：
   - 复用平时实验的词法分析器（推荐直接用 C++ 配合正则表达式库 <regex> 手写，或者将原来手工写的 DFA 封装成一个 Lexer 类）。
   - **严格适配大作业要求**：将源码转换为 Token 对象序列。每个 Token 必须包含：种别（KW, OP, SE, IDN, INT, FLOAT）、属性值（如 "10", "+"）、**行号**（极其重要，用于后续报错）。
   - **符号表雏形**：在遇到 IDN 时，将其填入一个全局或类的 SymbolTable 中。
3. **【重中之重】定义 AST 节点基类（为后三天铺路）**：
   - 必须在今天写出一个 ASTNode.h。定义出所有语法树节点的类，比如：ProgramNode, VarDeclNode, BinaryExpNode, IfStmtNode 等。
   - 提供虚拟的 accept(Visitor* v) 方法（为 D 同学的中间代码生成打好基础）。

**📦 产出物 (Gitee Push)：**

- Lexer.h / Lexer.cpp
- ASTNode.h (极其关键的接口文件)
- 测试程序 main.cpp（能够打印出合规的词法 Token 流）。
- *Commit 规范：* [Day1-A] 完成项目初始化、词法分析器封装及AST节点定义

------



### 🕒 Day 2 (5月15日) - SLR(1) 核心算法之“最强大脑”

👨‍💻 **负责人：同学 B**
⏰ **今日 DDL：** 5月15日 23:59 前 push 到 Gitee。

**【核心任务】**
今天只做算法，不生成 AST。你的任务是给明天的 C 同学提供一张**完美的 SLR 预测分析表**。

1. **文法数据结构硬编码**：将大作业附录 1 中的 36 条 C-- 文法，硬编码到 C++ 的数据结构中（例如 map<string, vector<vector<string>>>）。
2. **实现 FIRST 和 FOLLOW 集计算**：编写算法自动计算出非终结符的 FIRST 和 FOLLOW 集。
3. **构造 LR(0) 项目集规范族 (Closure & Goto)**：
   - 实现项目（Item）的数据结构：包含产生式编号、点号 . 的位置。
   - 实现 Closure() 和 Goto() 函数。
4. **生成 SLR 分析表**：
   - 根据 LR(0) 状态机和 FOLLOW 集，填充 ACTION 表（Shift, Reduce, Accept）和 GOTO 表。
   - **防坑指南**：处理好 Shift/Reduce 冲突（例如算符优先级和 dangling-else）。
   - 编写一个 printTable() 函数，将生成的 SLR 表打印成 .csv 格式，方便查错。

**📦 产出物 (Gitee Push)：**

- SLRTableGenerator.h / SLRTableGenerator.cpp
- 在 main.cpp 中连通 A 的代码，能够打印出 FIRST/FOLLOW 集和分析表。
- *Commit 规范：* [Day2-B] 完成LR(0)项目集构造与SLR分析表自动生成

------



### 🕒 Day 3 (5月16日) - SLR(1) 解析引擎与 AST 组装

👨‍💻 **负责人：同学 C**
⏰ **今日 DDL：** 5月16日 23:59 前 push 到 Gitee。

**【核心任务】**
拿着 A 传来的 Token 流，看着 B 做的分析表，执行推导并拼装 A 定义的 AST！

1. **编写 SLRParser 类**：
   - 维护两个栈：**状态栈**（存整数状态）和 **AST 节点符号栈**（存 ASTNode*，这是构建语法树的核心！）。
2. **执行 Shift (移进)**：
   - 当查表为 Shift 时，状态压栈；同时根据当前的 Token（如 INT_CONST 或 IDN），创建一个叶子节点（如 IntNode）压入 AST 节点符号栈。
   - **按要求打印动作**：严格打印 [序号] [TAB] [栈顶]#[面临符号] [TAB] move。
3. **执行 Reduce (规约) & 构建 AST**：
   - 当查表为 Reduce 时，根据产生式右部的长度 n，从状态栈弹出 n 个状态，从 AST 节点栈弹出 n 个节点。
   - **合并节点**：用弹出的 n 个节点作为子节点，创建一个新的父节点（例如弹出 E, +, T，创建一个 BinaryExpNode），并将新节点压回 AST 节点栈。
   - **按要求打印动作**：严格打印 [序号] [TAB] [栈顶]#[面临符号] [TAB] reduction。
4. **错误处理**：查表为空时，抛出带有行号的语法错误。

**📦 产出物 (Gitee Push)：**

- SLRParser.h / SLRParser.cpp
- 修改 main.cpp，使得程序跑完后，能够输出规约序列，并在内存中保有一棵完整的抽象语法树。
- *Commit 规范：* [Day3-C] 完成SLR移进规约引擎与抽象语法树(AST)构建

------



### 🕒 Day 4 (5月17日) - 中端对接与 LLVM IR 生成

👨‍💻 **负责人：同学 D**
⏰ **今日 DDL：** 5月17日 23:59 前 push 到 Gitee。

**【核心任务】**
不要怕 LLVM，官方已经给了中端 API！你只需要实现 Visitor 模式。

1. **阅读并引入官方 API**：拉取老师 Gitee 上的中端代码（附录2.2），将其加入到你们的项目工程中并调通编译。
2. **编写 IRGeneratorVisitor 类**：
   - 继承一个 Visitor 基类，实现对 C 同学生成的 AST 树的遍历。
   - 参考大作业 PDF 中给出的“代码 1”和“代码 2”伪代码，重写所有 visitXXX 函数。
3. **核心翻译逻辑**：
   - **变量声明**：调用官方 API 生成局部/全局变量，并利用符号表（存名称和 llvm::Value* 映射）管理变量作用域。
   - **二元运算**：遍历左右子节点，获取返回值，调用加减乘除 API 生成指令。
   - **控制流 (If/Else)**：调用官方提供的 BasicBlock 生成和跳转指令。
4. **最终输出**：将生成的 LLVM IR 打印到控制台或 .ll 文件中。

**📦 产出物 (Gitee Push)：**

- IRGenerator.h / IRGenerator.cpp
- 包含中端 API 库。
- *Commit 规范：* [Day4-D] 实现Visitor模式遍历AST，成功对接中端API生成LLVM IR

------



### 🕒 Day 5 (5月18日) - 拓展拔高、测试与报告撰写

👨‍💻 **负责人：同学 A 主导，全体协作**
⏰ **今日 DDL：** 5月18日 23:59 前完成所有工作。

**【核心任务】**
代码虽然跑通了，但要拿高分，今天必须做两件事：**包装** 和 **拓展**。

**🔥 拓展任务（加分项，A 同学主写代码）：**

1. **AST 树可视化 (Graphviz)**：写一个简单的函数，遍历 AST 生成 .dot 文件，导出极其漂亮的语法树图片放到报告里，老师绝对给高分！
2. **语法错误恢复 (Panic Mode)**：当 C 同学的 Parser 遇到错误时不要直接退出，而是丢弃几个 Token 直到遇到分号 ; 或 }，然后恢复解析，输出多条错误信息。
3. **简单的死代码消除/常量折叠**：在 D 同学生成 IR 之前，如果发现 AST 节点是 3 + 4，直接在内存里优化成 7 的节点。

**📝 文档任务（B、C、D 各写各的部分）：**

- **B 同学**：撰写开发报告中的“SLR 分析表的算法描述”（First/Follow怎么算的，项目集怎么合并的）。
- **C 同学**：撰写“AST 构建逻辑及移进规约序列输出说明”，提供成功解析和报错的测试截图。
- **D 同学**：撰写“Visitor 遍历过程及中端 API 调用逻辑”，截图最终生成的 .ll 代码。
- **A 同学**：汇总报告，撰写组员贡献度说明（严禁模糊，写清楚每日 DDL 的执行情况），排版 PPT。

**📦 产出物 (Gitee Push / 智慧树提交)：**

- 最终版代码整合。
- 《开发报告》、《测试报告》、答辩 PPT。
- *Commit 规范：* [Day5-Team] 增加AST可视化拓展功能，完善所有测试用例，准备提交

------



## ⚠️ 团队排雷与注意事项（组长 A 必看）

1. **接口灾难**：Day 3 (C) 极其依赖 Day 1 (A) 的 ASTNode 类。如果 A 在第一天没把类的属性（比如左右子节点指针、Token 值）定义好，C 第三天根本没法拼装。**A 必须在 Day 1 晚上和 C 开个 10 分钟短会确认接口。**
2. **环境灾难**：Day 4 (D) 要对接官方中端库，极有可能遇到 C++ 版本报错、依赖找不到的问题。**建议 D 同学不要等到第四天才看官方 API**，前三天可以提前自己建个测试项目把环境跑通。
3. **不要使用 Flex/Bison**：大作业明确规定“不得借助 Lex/Yacc 等自动生成工具”。一旦被查出使用了生成工具，直接 0 分。必须是纯 C++ 代码。
4. **C++ 指针内存泄漏**：在构建 AST 树时会大量使用 new。如果时间紧，可以直接不管 delete（现代系统跑完进程就回收了），或者使用 std::shared_ptr 防止野指针。