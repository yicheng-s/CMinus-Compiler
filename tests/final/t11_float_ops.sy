// t11: float运算 — A(FLOAT词法) C(float产生式) D(sitofp/fptosi IR转换)
int main() {
    int a = 10;
    float b = 3.5;
    float c;
    int d;
    c = b + 2.5;
    c = b * 2.0;
    d = a + 1;
    return d;
}
